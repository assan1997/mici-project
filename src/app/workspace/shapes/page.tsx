import { Shape } from "@/components/sections/shapes";
import MainContainer from "@/components/sections/shared/MainContainer";
const Home = () => {
  return (
    <MainContainer title="Formes">
      <Shape />
    </MainContainer>
  );
};

export default Home;
